name: Sync Godot Releases

on:
  schedule:
    - cron: "0 3 * * *"

  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Restore cache
        uses: actions/cache@v4
        with:
          path: versions-cache.json
          key: versions-cache-${{ hashFiles('versions.zon') }}
          restore-keys: |
            versions-cache-

      - name: Run sync
        run: ./sync.sh

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet versions.zon; then
            echo "changed=false" >> $GITHUB_OUTPUT
          else
            echo "changed=true" >> $GITHUB_OUTPUT
          fi

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add versions.zon
          git commit -m "chore: sync godot releases"
          git push
